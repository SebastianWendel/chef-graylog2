#
# Generated by Chef for <%= node['FQDN'] %>
# Local modifications will be overridden
#

general:
  external_hostname: "<%= (node['EC2'] && node['EC2']['PUBLIC_HOSTNAME']) || node['FQDN'] || "localhost" %>"
  date_format: "%d.%m.%Y - %H:%M:%S"
  allow_deleting: false
  allow_version_check: true 
  
subscriptions:
  from:  <%= node['GRAYLOG2']['EMAIL_ADDRESS'] %>
  subject: "[graylog2] Subscription"

streamalarms:
  from: <%= node['GRAYLOG2']['EMAIL_ADDRESS'] %>
  subject: "[graylog2] Stream alarm!"

hoptoad:
  enabled: false
  ssl: false
  api_key: 123
